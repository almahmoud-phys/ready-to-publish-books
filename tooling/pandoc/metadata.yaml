# tooling/pandoc/metadata.yaml — pandoc defaults for EPUB builds (per-book values injected from exports/metadata.json)
lang: en
css: tooling/pandoc/epub.css
toc-depth: 2
# Do not emit empty rights, cover, creator, publisher, or subject values. EPUB 3.3
# rejects an empty dc:rights element, and the remaining fields are injected only
# after their human-owned values are approved.
